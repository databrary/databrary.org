- args:
    enable_manual: false
    headers: []
    name: updatePrimaryEmail
    retry_conf:
      interval_sec: 10
      num_retries: 0
      timeout_sec: 60
    table:
      name: users
      schema: public
    update:
      columns:
      - email_primary
    webhook: http://localhost:8000/webhooks/email
  type: create_event_trigger
